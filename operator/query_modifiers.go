package operator

// Modifiers
const (
	// Comment = "$comment" // Declared
	Explain = "$explain"
	Hint    = "$hint"
	// Max     = "$max" // Declared
	MaxTimeMS = "$maxTimeMS"
	// Min       = "$min" // Declared
	OrderBy     = "$orderby"
	Query       = "$query"
	ReturnKey   = "$returnKey"
	ShowDiskLoc = "$showDiskLoc"
)

// Sort Order
const (
	Natural = "$natural"
)
